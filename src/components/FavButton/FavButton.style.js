import styled from 'styled-components';

const StyledFavButton = styled.button`
  padding-top: 8px;
  display: flex;
  cursor: pointer;
  align-items: center;
  & svg {
    margin-right: 4px;
  }
`;

export default StyledFavButton;
